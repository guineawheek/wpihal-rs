[package]
name = "build-test"
version = "0.1.0"
edition = "2024"

[features]
robot-controller = ["wpihal/robot-controller"]

[dependencies]
wpihal = { path = "..", default-features = false, features = ["shared"] }